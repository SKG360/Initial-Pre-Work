// Summary Comment - Random numbers are useful for creating random behavior.

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
console.log(randomFraction);
